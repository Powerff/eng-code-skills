# 通用代码评审 示例

## 场景
演示本技能在典型输入上的期望产出形态。

## 输入（Before）

```text
export function divide(a, b) {
  return a / b;
}
```

## 期望产出要点

```text
（评审意见示例，默认不改代码）
[必须关注] b===0 时产生 Infinity/异常，调用方契约未定义 — 标记风险，不擅自改返回值
[建议] 补充参数类型与单元测试
```

## 说明
评审以意见为主；逻辑缺陷只标记。

## 提醒
所有结论仅供参考，上线前必须人工评审与测试。
