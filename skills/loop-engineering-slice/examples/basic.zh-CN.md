# 示例：loop-engineering-slice

## 输入

```text
@loop-engineering-slice
切片 S0：Node 18 工程骨架、validate-skills.mjs 占位、npm run validate 通过。
Done：npm run validate 退出码 0。
```

## 预期

完成 L0–L4，附验证证据，S0 文件后 graphify update，已停服，不 commit。
